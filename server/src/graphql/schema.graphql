type Query {
    Recepies(ingredients: [String], limit: Int, offset: Int): [Recepie]
}

type Recepie {
    instruction: [String]
    ingredients: [Ingredients]
    name: String
}

type Ingredients {
  name: String,
  quantity: Float
}